import React from "react"
import PropTypes from "prop-types"

const Home = (props) => {
  return (
    <section style={{ minHeight: "500px" }}>
      <h2>Home</h2>
    </section>
  )
}

Home.propTypes = {}

export default Home
